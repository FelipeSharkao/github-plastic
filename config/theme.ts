import { DefaultTheme } from 'styled-components'

export const defaultTheme: DefaultTheme = {
  color: {
    base: { bg: '#FFFAE3', fg: '#191919' },
    muted: { bg: '#FFFAE3', fg: '#414141' },
    primary: { bg: '#DD403A', fg: 'white' },
    secondary: { bg: '#8390FA', fg: 'white' },
  },
}
